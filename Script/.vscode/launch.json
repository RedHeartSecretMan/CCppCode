{
    "version": "0.2.0",
    "configurations": [
        {
            "label": "C/C++: clang 生成活动文件",
            "type": "lldb",
            "request": "launch",
            "name": "WangHao Debug",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}